
<% @opinions.each do |o| %>
<div class="post_card">
<div class="avatar">
<%= check_photo(opinion.author) %>
</div>
<div>
 <%= link_to  opinion.author.fullname, user_path(opinion.author),class:"bold"  %>
<p><%= opinion.text%></p>
</div>
</div>
<%end%>